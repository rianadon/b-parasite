# Shared bring-up overlay for b-parasite samples.
#
# Applied by `scripts/build.sh <sample> ... --dev`. Pairs with Zephyr's
# upstream `cdc-acm-console` snippet (which build.sh stacks on top) for
# USB CDC ACM console routing. This snippet just adds verbose logging
# and a shrunk sleep cadence so updates land fast on the bench.
# Not for battery deployment — USB stack costs RAM and burns power.

# Verbose logs from prstlib + Zephyr defaults.
CONFIG_LOG_DEFAULT_LEVEL=4
CONFIG_PRSTLIB_LOG_LEVEL_DBG=y

# Shrunk sleep cadence — each sample's sleep-duration Kconfig picks a
# bench-friendly default when this is on (ble: 5 s, zigbee: 10 s).
# Defined in prstlib/Kconfig.
CONFIG_PRSTLIB_DEV_FAST_LOOP=y
